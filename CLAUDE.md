@AGENTS.md


## 백업 위치 규칙 (절대 준수)

이 프로젝트(`flydronemap`) 작업 전 백업을 생성할 때는 반드시 **이 폴더 내부**에 만든다: `.backups/backup_YYYYMMDD_HHMMSS`
상위 폴더(`Desktop/애드센스 제휴 마케팅/`)에 직접 백업 폴더를 만들지 않는다 (예: `../flydronemap_backup_...`, `../_backups/flydronemap_backup_...` 전부 금지).

## push용 .command 스크립트 — 자동 종료(osascript close) 시 확인 팝업 뜨는 문제, 근본 해결됨 (2026-09-02)

`push_실행.command`류 스크립트 끝에서 `osascript -e 'tell application "Terminal" to close (every window ...)'`로
터미널 창을 자동 닫을 때, "이 창에서 실행 중인 프로세스를 종료하시겠습니까? (bash, osascript)" 확인 팝업이
뜬 적이 있었음. **원인**: 터미널 앱(Terminal.app) 자체의 Settings → Profiles → Shell 탭에 있는
"닫기 전에 확인"(Ask before closing) 설정이 기본값(로그인 셸 외의 프로세스가 있으면 확인)으로 되어 있어서,
스크립트가 자기 자신이 실행 중인 창을 강제로 닫으려 할 때마다 터미널이 확인을 요구했던 것 — 스크립트
코드 자체의 문제가 아니라 터미널 앱 설정 문제였음.

**해결 완료(사용자가 직접 적용, 2026-09-02)**: 터미널 Settings → Profiles → 기본 프로파일 → Shell 탭 →
"닫기 전에 확인"을 "안 함"(Never)으로 변경. 이후 순수 테스트 스크립트(`자동종료_테스트.command`,
git 이력에는 없음)로 재검증한 결과 팝업 없이 3초 후 정상적으로 창이 자동으로 닫히는 것을 확인함.

## push용 .command 스크립트 — 더블클릭이 중복 인식되어 두 인스턴스가 겹쳐 실행되는 경우 (2026-09-03 발견)

더블클릭이 간혹 두 번 인식되어 `push_실행.command`가 거의 동시에 두 프로세스로 실행된 사례 발견 — 실제 push
자체는 문제없이 성공했으나(두 번째 인스턴스는 `git push`가 "Everything up-to-date"로 나옴), 두 인스턴스가
동시에 같은 파일을 `rm -- "$0"`으로 지우려다 경합이 생겨 파일이 지워지지 않고 남는 현상이 있었음.

## push용 .command 스크립트 — 자체 삭제 순서를 "터미널 종료 이후"에서 "터미널 종료 이전"으로 정정 (2026-09-03, 중요)

**과거(위 2026-09-02 항목)에는 "완료 메시지 → 3초 대기 → osascript로 창 닫기 → 자체 삭제(`rm -f -- "$0"`)"
순서를 표준으로 명문화했었으나, 이 순서는 틀렸다.** 실제로 이 순서를 쓴 스크립트가 "push는 정상 성공,
터미널도 정상 종료됐지만 스크립트 파일 자체는 삭제되지 않고 남는" 증상을 보임을 재확인함(2026-09-03).
`osascript`로 창을 닫으면 그 창에서 실행 중이던 셸(=스크립트 자신의 프로세스)이 그대로 종료되어 버려서,
그 뒤에 있는 `rm -f -- "$0"` 줄이 실행될 기회를 못 얻는 경우가 있는 것으로 보임 — 위 확인 팝업 문제(창을
강제로 닫을 때 뜨는 팝업)와는 별개의, 창 닫기 자체가 갖는 타이밍 문제. 반대로 **자체 삭제를 먼저 하고
그다음에 창을 닫는 스크립트에서는 삭제가 안정적으로 성공함**을 사용자가 직접 확인해줌 — 유닉스에서는
실행 중인 파일을 먼저 지워도(이미 셸에 읽혀 들어간 상태라) 그 실행 자체는 끝까지 문제없이 진행되므로,
삭제를 먼저 해도 안전함.

**정정된 표준 순서 (이후 새로 만드는 모든 push 스크립트는 반드시 이 순서를 따를 것)**:
1. git commit + push 완료
2. 완료 메시지 출력
3. `rm -f -- "$0" 2>/dev/null || true` (자체 삭제 — 이미 위 2026-09-03 더블클릭 경합 이력을 반영해
   `|| true`로 이미 삭제된 경우에도 에러 없이 넘어가도록 처리)
4. `sleep 3`
5. `osascript`로 터미널 창 닫기 — 창 이름이 아니라 현재 세션의 tty(`tty` 명령 결과)로 정확히 매칭해,
   중복 실행 시 서로 다른 인스턴스의 창을 잘못 닫는 일이 없도록 함(2026-09-03 더블클릭 경합 이력 반영)

이 순서는 더블클릭 중복 실행 경합 문제도 함께 완화한다 — 먼저 지워버리면 설령 두 인스턴스가 동시에
실행되더라도 두 번째 인스턴스는 이미 없는 파일을 지우려 시도할 뿐이라(`|| true`로 에러 없이 넘어감)
문제가 되지 않는다.
